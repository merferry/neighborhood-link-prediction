\begin{figure*}[hbtp]
  \centering
  \subfigure[Runtime in seconds (logarithmic scale) for link prediction using various similarity measures, with \textit{IHub} approach]{
    \label{fig:standard2--runtime}
    \includegraphics[width=0.98\linewidth]{out/standard2-runtime.pdf}
  }
  \subfigure[F1 score of predicted links (logarithmic scale) for link prediction using various similarity measures, with \textit{IHub} approach]{
    \label{fig:standard2--f1score}
    \includegraphics[width=0.98\linewidth]{out/standard2-f1score.pdf}
  } \\[-2ex]
  \caption{Runtime (log-scale) and F1 score (log-scale) for link prediction using various similarity measures with the \textit{IHub} approach, when attempting to predict $10^{-2}|E|$ unobserved edges $E^U$ for each graph in the dataset.}
  \label{fig:standard2}
\end{figure*}
